# EstateSaleFinder.ai Backend Environment Variables
# Copy this file to .env and update with your values

# Database Configuration
DB_USER=postgres
DB_PASS=postgres
DB_NAME=estatesale_db
DB_HOST=postgres
DB_PORT=5432

# Server Configuration
PORT=8080
APP_ENV=local
LOG_LEVEL=info

# CORS Configuration
CORS_ALLOWED_ORIGIN=http://localhost:3000

# Google Cloud Configuration
GCLOUD_PROJECT=your-gcloud-project-id
REGION=us-west1

# Firebase (credentials file in ./credentials/firebase-adminsdk.json)
# No env var needed - uses file path in code
